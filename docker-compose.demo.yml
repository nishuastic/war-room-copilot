services:
  dashboard:
    build:
      context: ./frontend
      args:
        VITE_API_URL: "/api"
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/nginx-demo.conf:/etc/nginx/conf.d/default.conf:ro
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: "no"
