services:
  livekit-server:
    image: livekit/livekit-server:latest
    command: --dev --bind 0.0.0.0 --config /etc/livekit.yaml --node-ip ${NODE_IP}
    ports:
      - "7880:7880"   # WebSocket + HTTP API
      - "7881:7881"   # WebRTC TCP
      #- "8000:8000"   # Dashboard API on agent
      - "50000-50060:50000-50060/udp"  # WebRTC UDP (ICE candidates)
    volumes:
      - ./livekit.yaml:/etc/livekit.yaml:ro
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:7880"]
      interval: 5s
      timeout: 3s
      retries: 5

  github-mcp-server:
    image: ghcr.io/github/github-mcp-server
    command: ["http", "--port", "8090"]
    environment:
      - GITHUB_PERSONAL_ACCESS_TOKEN=${GITHUB_TOKEN}
    expose:
      - "8090"
    restart: unless-stopped

  agent:
    build: .
    env_file: .env
    environment:
      - LIVEKIT_URL=ws://livekit-server:7880
      - LIVEKIT_API_KEY=devkey
      - LIVEKIT_API_SECRET=secret
      - GITHUB_MCP_URL=http://github-mcp-server:8090/mcp
    ports:
      - "8000:8000"  # Dashboard API
    volumes:
      - speaker-data:/app/data   # voiceprint + postmortem persistence
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    depends_on:
      github-mcp-server:
        condition: service_started
    restart: unless-stopped

volumes:
  speaker-data:
